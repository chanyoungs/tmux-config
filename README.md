# tmux configuration

Tmux configuration file.

## Installation

Run the install script to symlink `.tmux.conf` to your home directory:

```bash
./install.sh
```

This will create `~/.tmux.conf -> <repo>/.tmux.conf`. If a file already exists at `~/.tmux.conf`, it will be backed up to `~/.tmux.conf.bak`.
